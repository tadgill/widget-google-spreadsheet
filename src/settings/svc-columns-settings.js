(function() {

  "use strict";

  angular.module("risevision.widget.common.googleSpreadsheet.settings")
    .factory("columnsService", ["visualizationApi", "$q", "$log", function (visualizationApi, $q, $log) {

      // TODO: functions for querying and returning data using google visualization api

    }]);

})();
